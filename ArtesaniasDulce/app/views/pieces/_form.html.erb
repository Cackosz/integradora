<%= form_for(@piece) do |f| %>
  <% if @piece.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@piece.errors.count, "error") %> prohibited this piece from being saved:</h2>

      <ul>
      <% @piece.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :piece_name %><br>
    <%= f.text_field :piece_name %>
  </div>
  <div class="field">
    <%= f.label :piece_quantity %><br>
    <%= f.number_field :piece_quantity %>
  </div>
  <div class="field">
    <%= f.label :price %><br>
    <%= f.text_field :price %>
  </div>
  <div class="field">
    <%= f.label :matter_name %><br>
    <%= f.text_field :matter_name %>
  </div>
  <div class="field">
    <%= f.label :category_id %>
    <%= f.collection_select(:category_id, Category.all, :id, :type_category) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
